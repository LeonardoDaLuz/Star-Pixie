using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tste : MonoBehaviour {

	// Use this for initialization
	void Start () {
		GetComponent<CanvasRenderer>().SetAlpha(0.5f);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
